# 6502retrofun
Collection of 6502 assembly source projects targeting 8-bit machines

To build all the targets, run `make` without arguments.
This will require Linux with [cc65](https://cc65.github.io/) built and located at $CC65_HOME and available in the $PATH along with GNU make.